{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",                                   /* Specify an output folder for all emitted files. */
    "target": "ES2019",
    "lib": [ "ES2019"],
    "baseUrl": ".",
    "paths": {
      "@/*": ["*"]
    }
  },
  "exclude": [
    "node_modules",
    "dist"
  ]
}
